import styled from 'styled-components/native';
import { m3, m4 } from '../../ui/theme';

export const HomeHeadWrapper = styled.View`
  margin-top: ${m4};
  margin-bottom: auto;
  padding-left: ${m3};
`;
