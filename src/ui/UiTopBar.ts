import styled from 'styled-components/native';

import { m3 } from './theme';

export const UiTopBar = styled.View`
  margin-top: ${m3};
  padding: ${m3};
  flex-direction: row;
  justify-content: space-between;
`;
